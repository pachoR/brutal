import React from 'react';
import Header from '../../components/Header';

const Main: React.FC = () => {
    return (
        <div style={{width: '100%', height: '100%'}}>
            <Header/>
        </div>
    )
}

export default Main;